# Copy to .env (never commit .env). Read by the pipeline at startup.

# Adzuna API credentials (https://developer.adzuna.com/). Leave empty to run on fixture data.
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# Optional overrides
# JOBMARKET_DB=var/jobmarket.sqlite
# JOBMARKET_ENV=production
